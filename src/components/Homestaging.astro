<section class="homestaging section-padding" style="background: var(--grey);">
  <div class="container">
    <p class="section-label">Homestaging</p>
    <h2 class="section-headline">Der erste Eindruck entscheidet</h2>
    <p class="section-subtext">Kostenlos für Sie: Wir inszenieren Ihre Räume so, dass Käufer sich sofort zuhause fühlen.</p>
    <div class="homestaging-grid">
      <div class="homestaging-item">
        <span class="homestaging-badge">Vorher</span>
        <div class="homestaging-placeholder">Vorher-Bild<br/>Homestaging</div>
      </div>
      <div class="homestaging-item">
        <span class="homestaging-badge">Nachher</span>
        <div class="homestaging-placeholder">Nachher-Bild<br/>Homestaging</div>
      </div>
    </div>
  </div>
</section>

<style>
  .homestaging-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin-top: 2.5rem;
  }
  .homestaging-item {
    position: relative;
    overflow: hidden;
  }
  .homestaging-placeholder {
    width: 100%;
    aspect-ratio: 3/2;
    background: var(--accent-pri);
    color: var(--accent-sec);
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    font-size: 0.9rem;
    font-weight: 400;
  }
  .homestaging-badge {
    position: absolute;
    top: 1rem;
    left: 1rem;
    background: var(--dark);
    color: var(--light);
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 0.35rem 0.85rem;
    border-radius: 30px;
    z-index: 1;
  }
  @media (max-width: 767px) {
    .homestaging-grid { grid-template-columns: 1fr; }
  }
</style>
